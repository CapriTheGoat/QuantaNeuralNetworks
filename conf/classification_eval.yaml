# conf/eval.yaml
defaults:
  - hydra
  - model: classification
  - data: spad_mnist
  - override hydra/job_logging: none
  - _self_

ckpt_path: ${hydra:runtime.cwd}/data/ckpt/${model.name}/

hydra:
  output_subdir: null
  run:
    dir: .
