# conf/eval.yaml
defaults:
  - hydra
  - model: classification_morphed_priority
  - data: spad_mnist
  - override hydra/job_logging: none
  - _self_

ckpt_path: ${hydra:runtime.cwd}/outputs/training_run/classification/model.ckpt.folder=classification_morph_priority,model=classification/${model.name}/

hydra:
  output_subdir: null
  run:
    dir: .
